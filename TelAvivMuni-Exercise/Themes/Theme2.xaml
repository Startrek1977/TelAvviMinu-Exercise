<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:TelAvivMuni_Exercise.Controls">

  <!-- ============================================ -->
  <!-- Theme2 - Teal/Green Theme Color Palette -->
  <!--
    To use this theme, update App.xaml line 10:
    <ResourceDictionary Source="/Themes/Theme2.xaml" />
  -->
  <!-- ============================================ -->

  <!-- Primary Colors (Teal/Green Theme) -->
  <SolidColorBrush x:Key="PrimaryBrush" Color="#009688" />
  <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#00796B" />
  <SolidColorBrush x:Key="PrimaryDarkerBrush" Color="#00695C" />
  <SolidColorBrush x:Key="PrimaryLightBrush" Color="#B2DFDB" />
  <SolidColorBrush x:Key="PrimaryExtraLightBrush" Color="#E0F2F1" />

  <!-- Secondary Colors (Dark Gray) -->
  <SolidColorBrush x:Key="SecondaryBrush" Color="#455A64" />
  <SolidColorBrush x:Key="SecondaryDarkBrush" Color="#37474F" />
  <SolidColorBrush x:Key="SecondaryDarkerBrush" Color="#263238" />

  <!-- Neutral/Gray Colors -->
  <SolidColorBrush x:Key="NeutralLightBrush" Color="#B0BEC5" />
  <SolidColorBrush x:Key="NeutralMediumBrush" Color="#78909C" />
  <SolidColorBrush x:Key="NeutralDarkBrush" Color="#607D8B" />
  <SolidColorBrush x:Key="NeutralExtraLightBrush" Color="#ECEFF1" />
  <SolidColorBrush x:Key="NeutralHoverBrush" Color="#E0F2F1" />

  <!-- Border Colors -->
  <SolidColorBrush x:Key="BorderBrush" Color="#00796B" />
  <SolidColorBrush x:Key="BorderLightBrush" Color="#B2DFDB" />

  <!-- Text Colors -->
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="#263238" />
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="#37474F" />
  <SolidColorBrush x:Key="TextTertiaryBrush" Color="#607D8B" />
  <SolidColorBrush x:Key="TextDisabledBrush" Color="#78909C" />

  <!-- Disabled State -->
  <SolidColorBrush x:Key="DisabledBrush" Color="#B0BEC5" />

  <!-- White -->
  <SolidColorBrush x:Key="WhiteBrush" Color="White" />

  <!-- ============================================ -->
  <!-- Theme Resources and Styles -->
  <!-- ============================================ -->

  <!-- Browse Dialog Icon (same as browse button content) -->
  <DrawingImage x:Key="BrowseDialogIcon">
    <DrawingImage.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="Transparent">
          <GeometryDrawing.Pen>
            <Pen Brush="White" Thickness="1.5"/>
          </GeometryDrawing.Pen>
          <GeometryDrawing.Geometry>
            <PathGeometry Figures="M 2,1 H 14 A 1,1 0 0 1 15,2 V 5 A 1,1 0 0 1 14,6 H 2 A 1,1 0 0 1 1,5 V 2 A 1,1 0 0 1 2,1 Z M 2,10 H 14 A 1,1 0 0 1 15,11 V 14 A 1,1 0 0 1 14,15 H 2 A 1,1 0 0 1 1,14 V 11 A 1,1 0 0 1 2,10 Z"/>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
        <GeometryDrawing Brush="White">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <EllipseGeometry Center="4,3.5" RadiusX="1" RadiusY="1"/>
              <EllipseGeometry Center="4,12.5" RadiusX="1" RadiusY="1"/>
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingImage.Drawing>
  </DrawingImage>

  <!-- Base Button Style -->
  <Style x:Key="BaseButtonStyle" TargetType="Button">
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  CornerRadius="8"
                  Padding="20,8">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Blue Button Style (Primary actions) - Using Teal/Green -->
  <Style x:Key="BlueButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="{StaticResource PrimaryDarkerBrush}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Background" Value="{StaticResource DisabledBrush}" />
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- Gray Button Style (Secondary actions) - Using Dark Gray -->
  <Style x:Key="GrayButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
    <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
    <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource SecondaryDarkBrush}" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="{StaticResource SecondaryDarkerBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- Clear Button Style (Small circular button with X) -->
  <Style x:Key="ClearButtonStyle" TargetType="Button">
    <Setter Property="Width" Value="20" />
    <Setter Property="Height" Value="20" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Content" Value="&#x2715;" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{StaticResource NeutralDarkBrush}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  CornerRadius="10">
            <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{StaticResource NeutralHoverBrush}" />
              <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Browse Button Style (... button for DataBrowserBox) -->
  <Style x:Key="BrowseButtonStyle" TargetType="Button">
    <Setter Property="Width" Value="35" />
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="2,0,0,0"
                  CornerRadius="0,4,4,0"
                  Padding="4">
            <Viewbox Width="20" Height="20">
              <Path Data="M 4,2 H 20 A 2,2 0 0 1 22,4 V 8 A 2,2 0 0 1 20,10 H 4 A 2,2 0 0 1 2,8 V 4 A 2,2 0 0 1 4,2 Z M 4,14 H 20 A 2,2 0 0 1 22,16 V 20 A 2,2 0 0 1 20,22 H 4 A 2,2 0 0 1 2,20 V 16 A 2,2 0 0 1 4,14 Z M 6,6 L 6.01,6 M 6,18 L 6.01,18"
                    Stroke="{TemplateBinding Foreground}"
                    StrokeThickness="2"
                    Fill="Transparent"
                    Width="24"
                    Height="24"
                    Stretch="Uniform"/>
            </Viewbox>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="{StaticResource PrimaryDarkerBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- TextBox Style for DataBrowserBox display -->
  <Style x:Key="DataBrowserTextBoxStyle" TargetType="TextBox">
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="IsReadOnly" Value="True" />
    <Setter Property="Padding" Value="8,0,28,0" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Style.Triggers>
      <Trigger Property="Opacity" Value="0.5">
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontStyle" Value="Italic" />
        <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}" />
      </Trigger>
      <Trigger Property="Opacity" Value="1">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- DataGrid Column Header Style -->
  <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Padding" Value="12,10" />
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
    <Setter Property="BorderThickness" Value="0,0,0,2" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
  </Style>

  <!-- DataGrid Cell Style -->
  <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="DataGridCell">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryDarkerBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- DataGrid Row Style -->
  <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
    <Setter Property="Background" Value="{StaticResource WhiteBrush}" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource PrimaryExtraLightBrush}" />
      </Trigger>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}" />
      </Trigger>
    </Style.Triggers>
  </Style>

</ResourceDictionary>
